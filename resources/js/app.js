import { computePosition, flip, shift, offset, autoUpdate } from '@floating-ui/dom';
import './field.js';

window.FloatingUIDOM = {
    computePosition,
    flip,
    shift,
    offset,
    autoUpdate
};
